# Node modules
/node_modules

# Compilation output
/dist

# pnpm deploy output
/bundle

# Hardhat Build Artifacts
/artifacts

# Hardhat compilation (v2) support directory
/cache

# Typechain output
/types

# Hardhat coverage reports
/coverage

# Environment variables
.env
.env.local
.env.*.local
.env.keys
.env.keys.local
.env.keys.*.local
.env.keys.keys
.env.keys.keys.local
.env.keys.keys.*.local
.env.keys.keys.keys
.env.keys.keys.keys.local
.env.keys.keys.keys.*.local
.env.keys.keys.keys.keys
.env.keys.keys.keys.keys.local
.env.keys.keys.keys.keys.*.local

# Allow .env.example (template file)
!.env.example